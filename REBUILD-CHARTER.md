# 재구축 헌장 — 무엇이 틀렸고, 그래서 무엇을 다르게 하는가

2026-08-26. 제로베이스 재구축 전에 지금까지의 실수를 원인별로 묶고 그 원인을 막는 규칙을
정한다.

**이 문서는 2판이다.** 1판을 codex 에 방향 확인으로 넘겼고 [P1] 12건이 나왔다. 그중
사실 오류 여섯을 고쳤고, 판정 넷을 뒤집었고, 살아 있는 버그 셋은 코드에서 고쳤다. 1판이
틀린 자리는 지웠다 — 어디가 틀렸는지는 §6 에 남긴다.

근거는 `learnings.jsonl` 28건과 커밋 이력이다. 기억으로 쓰지 않았다.

---

## 0. 규칙을 더 만드는 것이 답이 아니라는 것부터

가장 크고 가장 안 보이는 실수. **2026-08-25 아침에 이 저장소를 process-as-proxy 라고
진단하고, 그날 하루 종일 게이트를 더 만들었다.** 기업 대화는 0건에서 0건이었다.

게이트는 진짜 결함을 잡았다. 그러나 막고 있던 것은 게이트 부족이 아니었다. Snowflake
회신 하나와, 아무도 외부에서 막지 않는 0a(기한 6일 지남)였다.

---

## 1. 여섯 가지 원인

### R1. 주장을 쓰고 검사를 안 쓴다 (9건 — 압도적 1위)

의도가 산문(docstring·PR 본문·커밋 메시지·CI 스텝 설명)에 있고 코드가 그걸 안 한다.

| 무엇을 주장했나 | 실제 |
|---|---|
| `test_suppression` 이 실행됐음을 증명하고 본 개수를 센다 | `seen` 이 증가만 하고 **어디에도 안 쓰였다.** 출력은 계산된 상수. 54상태 전부가 빈 파싱이어도 `PASS` |
| `--check` 가 "같은 시드는 같은 바이트" 를 확인한다 | 두 번 생성하지 않았다 |
| 생성기가 "부분 월 처리를 시험한다" | 입·퇴사가 월 경계뿐, 모든 행이 `month_days` 전체 |
| `make check` 가 CI 와 같은 게이트를 돌린다 | 두 개가 CI 에만 있었다 |
| `check_start_gates` 가 문서 불일치를 잡는다 | 정규식이 정답과 오답을 함께 받았다 |
| CI 스텝: 혼합이 `FRAC.activated`·`FRAC.repeat` 에서 파생된다 | `FRAC.repeat` 결합을 전날 없앴는데 설명을 안 고쳤다 |
| `test_pages_root` 가 리다이렉트를 검사한다 | 문서 전체 부분문자열. 주석 한 줄로 우회 |
| `build_pages_site` 가 발행 루트를 지킨다 | 그 검사가 교체될 파일 안에 있었다 |
| 내보내기 계약을 검사한다 | 키 존재만 봤다 |

공통점 하나. **현재 상태에서 통과하도록 썼고, 고장난 상태에서 실패하는 것을 본 적이 없다.**

### R2. 값이 적힌 곳을 한 군데만 읽는다 (6건)

- 정본이 `Repeat` 을 **두 번, 다른 분모로** 정의했다. 낡은 쪽을 읽고 "사양 충돌, 해소안
  4개" 라고 보고했다. T14 분모로 재면 충돌이 없었다.
- 문서에서 유도를 철회하고 **그 유도를 구현한 코드를 남겼다. 하루에 두 번.**
- §16 에 "다섯" 을 넣으면서 §17.1 의 "넷" 을 뒀다. 같은 커밋의 내 검사가 통과시켰다.
- **그 검사를 고친 뒤에도 정본 세 곳이 "넷" 으로 남았다.** 검사가 기술문서만 스캔했다.
  한 문서만 보는 검사는 두 문서가 어긋나는 것을 정의상 못 잡는다.
- "`.gitignore` 작은 충돌 하나" — 실제로 겹치는 파일이 다섯. 머지를 안 돌려보고 말했다.

### R3. 데이터 모델을 확인하고 실행 경로를 안 확인한다 (4건)

- "대시보드를 Snowflake 로 재배선" — `index.html` 은 `fetch` 0건이고 PRD §2.2 가 로그인을
  범위 밖으로 명시한다. **의존할 계층이 없었고 검토 두 번이 놓쳤다.**
- `gates.yml` 을 고친 브랜치가 PR 이 없어 그 workflow 를 한 번도 실행하지 않았다. 첫 CI 가
  11초에 죽었다. 로컬 열 개는 계속 초록불이었다.
- 커밋 해시를 해석하는 검사가 로컬 통과, CI 사망 (`fetch-depth: 1`).

### R4. 숫자가 맞으면 증명이라고 읽는다 (3건)

- `(v − a·r) + (a − v)` 가 ONE_SHOT 과 소수 네 자리까지 맞는다고, 우연이 아니라고 썼다.
  전개하면 `a − a·r` 이고 그것이 ONE_SHOT 이다. **v 가 무슨 값이든 성립하는 항등식.**
- 허용치 0.08 을 측정값 0.073 에 맞춰 골랐다.
- 36개월 전체를 분모로 써서 `repeat` 을 0.45 로 읽었다.

### R5. 눈에 보이는 일을 한다 (§0)

### R6. 병행 작업을 확인하지 않고 시작한다 (2026-08-26, 2건)

이 저장소는 여러 에이전트가 서로 다른 worktree 에서 동시에 작업한다. 그것을 모르고
시작해서 두 번 부딪혔다 — 한 번은 "결정만" 범위의 레인에 구현을 넣었고, 한 번은 작업 중에
main 에 들어온 같은 수정과 충돌해 절반을 버렸다. R3 와 같은 뿌리다: 실행 경로를 안 봤고,
여기서는 그 경로가 "이미 누가 하고 있나" 였다.

### 계수

R1 9 + R2 6 + R3 4 + R4 3 = **22건.** R6 둘은 2026-08-26 에 생겼고 학습 28건 밖이다. 나머지 6건은 도구 운영 지식이고
(codex stdin·browse 샌드박스·iPad hover·gstack pull·make-pdf 비결정성·PDF 스탬프 범위)
실수가 아니라 환경 사실이다. **1판은 "다섯이 28건 전부를 덮는다" 고 썼는데 21밖에
세지 않았다.** 이것도 R4 다 — 숫자를 안 세고 덮는다고 말했다.

---

## 2. 규칙 여섯

1판의 규칙은 codex 지적대로 셋이 원인을 못 덮었다. 고쳤다.

### G1. 검사는 고장을 먼저 재현하고 커밋한다 — 산문 주장도 포함

새 검사나 "이것을 보장한다" 는 주장을 커밋하기 전에 **그 검사가 실패하는 것을 실제로 본다.**
못 봤으면 커밋하지 않는다.

**1판이 놓친 것 (codex):** 실행되는 단언만 다뤘고 산문 주장은 안 다뤘다. CI 스텝 설명이
`FRAC.repeat` 을 말하는데 코드는 안 쓰는 상태가 그 증거다. **그래서 규칙을 넓힌다 —
docstring·커밋 메시지·CI 스텝 이름·PR 본문에 쓴 행위 주장은 그 자리에서 코드와 대조한다.**
못 대조하면 쓰지 않는다.

파생: 통과 조건에 실패 사례를 나열하지 않는다. 허용치를 측정값에 맞춰 고르지 않는다 —
근거를 못 쓰면 단언하지 말고 델타만 찍는다.

증거: 2026-08-25 밤 codex 지적 10건과 2026-08-26 지적 셋을 고칠 때 이 규칙을 지켰다.
매번 고장을 먼저 만들어 실패를 확인했다. **그 두 구간에서만 재작업이 안 나왔다.**

### G2. 값이 적힌 자리를 세고, 검사도 그 전부를 본다

**1판은 "값은 한 곳에만 손으로 적는다" 였고 codex 가 둘을 지적했다.** 중복만 다뤘고
불완전 탐색을 안 다뤘으며, "나머지는 생성하거나 대조한다" 는 이 문서가 거부하는 기계를
요구한다.

그래서 규칙을 행위로 바꾼다. **값·정의·개수를 주장하기 전에 그것이 적힌 자리를 전부
grep 해서 센다.** 그리고 그것을 검사로 만들 때는 **한 파일만 스캔하지 않는다** — 어제
`check_start_gates` 가 기술문서만 봐서 정본 세 곳을 놓쳤다.

파생: 문서에서 유도·정의·수치를 철회하면 **같은 커밋에서 그 산술 모양을 코드에서 grep 한다.**
산문만 고친 철회는 철회가 아니다.

### G3. 실행 경로 세 질문 + 대상 환경에서 실제로 돌린 증거

1. 호출 주체가 있는가 2. 인증이 범위 안인가 3. 자격증명이 어디 사는가

**1판이 놓친 것 (codex):** "PR 을 열었다" 가 "workflow 가 성공적으로 돌았다" 를 뜻하지
않는다. 그래서 넷째를 더한다 — **대상 환경에서 통과한 실행 하나를 인용한다.** run ID 든
출력이든.

### G4. 수치 일치는 기호로 전개한 뒤에만 근거가 된다

항등식은 아무것도 확인해주지 않는다. 창(window)과 분모는 지표마다 다르다.

*codex 판정: 원인과 일대일로 맞는 유일한 규칙.*

### G5. 막힌 것을 건드리지 않는 작업은 그렇다고 쓰고, 우선순위를 내린다

**1판은 "한 줄로 말한다" 였고 codex 가 정확히 짚었다** — "아니오" 라고 쓰고 하루를 더
쓰는 것을 허용한다. 그래서 결과를 붙인다. **막힌 것을 안 건드리는 작업은 막는 것을
건드리는 작업 뒤로 보낸다.** 그럴 수 없으면(내가 못 하는 일이면) 그것도 쓴다.

2026-08-25 에 이 규칙이 있었으면: "게이트 다섯 개는 #2·#31 을 건드리지 않는다. #31 의
0a 는 제가 못 씁니다." 그 두 줄이 하루를 다르게 만들었을 것이다.

### G6. 시작 전에 열린 PR 과 그 이슈의 범위를 본다

`gh pr list` 를 돌리고 이슈의 scope 줄을 읽는다. **이 저장소는 여러 에이전트가 서로 다른
worktree 에서 동시에 작업한다.** 2026-08-26 에 두 번 부딪혔다.

첫 번째는 이슈 #36 이 *"Scope: Decision only"* 로 명시돼 있고 PR #37 이 머지 결정을
기다리는 동안 구현을 만든 것이다. 결론이 같았던 것은 운이고, 판정 기준 하나(Chrome 렌더)를
몰라서 안 지킨 채로 머지됐다.

두 번째는 이 규칙을 쓰기 직전에 났다. #40 이 작업 중에 같은 수정을 main 에 넣었고,
제 PR 이 `DIRTY` 로 돌아와 절반을 버렸다. #40 판이 더 정확했다 — 새 게이트가 CI 명령을
하나 늘려 검증 단위가 21→22 가 된 것을 제 것은 놓쳤다.

**둘 다 `gh pr list` 한 번으로 막혔다.** 그리고 둘 다 push 한 뒤에 알았다.

따라오는 습관 둘.

- **이슈가 "결정만" 이라 쓰면 결정을 기록하고 멈춘다.** 구현은 별 이슈이고 별 검토다.
- **결정 기록과 구현이 어긋나면 기록이 명세다.** 코드를 옮기고 기록을 고치지 않는다.
  #39 를 그렇게 처리했다 — `canon.json` 을 별 파일로 바꾸고 검사에 렌더를 붙였다.

---

## 3. codex 를 어떻게 쓰는가

**결함 주장은 정확하고 처방은 신뢰하지 않는다.** 네 번 증명됐다.

정확했던 것: 2026-08-25 재검토 10건(내 자체 검토 0건, 게이트 12종 초록불), 2026-08-26
헌장 검토 12건 — 그중 `test_suppression` 이 파싱 0건으로 통과 가능하다는 것, 정본 세 곳이
아직 "넷" 이라는 것, CI 스텝 설명이 거짓이라는 것. **셋 다 살아 있는 버그였고 이 문서를
쓰는 동안 고쳤다.**

틀렸던 것:
- "Standard 아키텍처는 불가능" → 보안 뷰로 대체 가능함을 Snowflake 문서로 확인
- `repeaters` 를 `0.28 × 0.61` 로 고치라 → 그건 또 다른 보정. 맞는 수정은 혼합을
  `FRAC.repeat` 에서 **떼어내는** 것
- 헌장 검토에서 "서버 억제가 행을 없앤다는 것은 거짓" 이라며 정본 318행을 인용 →
  그 행은 억제 규칙이 아니다. **다만 결론은 맞았다** (§4.3 참조). 인용이 틀리고 판정이
  맞은 경우이므로 **인용을 확인하지 않으면 판정도 확인할 수 없다.**

규칙: **결함 주장은 단서로 받아 직접 검증하고, 수정은 내가 설계한다. 인용된 행 번호를
열어본다.**

검토자에게 산출물만 주면 동의한다. 경로를 주고 열어서 확인하라고 해야 나온다. 읽을 것과
읽지 말 것을 프롬프트에 적는다.

운영: `codex review --base` 는 바이너리 섞인 큰 diff 에서 330초에 죽는다. 실행 가능한
표면으로 좁혀 `codex exec` 로 간다. stdin 은 항상 `< /dev/null`.

---

## 4. 재구축 계획

### 4.1 화면 폐기 조건 — 제안서 재현이 아니라 게이트 넷

1판은 "새 화면이 같은 스크린샷 12장을 낼 수 있을 때" 라고 썼다. **codex 가 뒤집었고
맞다** — 그건 새 UI 를 발송된 PDF 에 묶는다. 제안서 v12 는 동결된 판매 산출물이고
호환 목표가 아니다.

**폐기 조건을 게이트로 바꾼다.** 헌 화면을 지우려면 넷이 먼저 서야 한다.

1. `doc-manifest.json` 의 스크린샷 입력이 헌 화면을 가리키지 않는다 — 안 고치면
   `test_doc_freshness` 가 깨진다 (codex 가 §4.3 미분류로 지적)
2. `check-forbidden-terms.sh` 가 새 화면 경로를 스캔한다 — 지금 `[ -f "$f" ] || continue`
   로 가드하므로 **경로가 사라지면 규제 스캔이 조용히 건너뛴다**
3. 정본 `surfaces` 가 새 경로를 가리킨다 — `check_surfaces` 가 없는 파일에서 실패한다
4. `test_consent` 가 새 임직원 화면에 대해 다시 서 있다

발송된 제안서와 그 이미지는 그대로 보존한다. 재현하지 않는다.

### 4.2 스택 — 정본을 빌드 시점에 읽되, 검사는 남긴다

**1판은 `test_single_source` 를 없앤다고 썼다. codex 가 뒤집었고 맞다.**

그 검사는 정본↔소스 일치를 보지 않는다. `index.html` 안의 리터럴 7개를 **변조하고**,
Chrome 으로 렌더하고, 바뀐 값이 DOM 에 도달했으며 옛 값이 없음을 단언한다. 빌드 시점
정본 읽기는 **손으로 옮기는 단계**만 없앤다. 템플릿·포매터·라벨·다른 컴포넌트가 옛 값을
품고 있는 것은 그대로 가능하다.

**그래서 없애지 않고 경계를 옮긴다.** 임시로 변조한 정본에 대해 **실제 빌더를 돌리고**,
빌드 산출물을 렌더해서 새 값이 나오고 옛 값이 없음을 단언한다. 같은 검사를 빌드 경계에서
다시 쓰는 것이다.

프레임워크 판단 기준: **정본을 읽는 경로가 손 작업 없이 서는가**, 그리고 **위 변조 검사를
붙일 수 있는가.**

**결정 — 프론트엔드 프레임워크를 넣지 않는다 (2026-08-26, #36).** 저장소가 이미 고정한
Python 3.14 와 PyYAML 로 작은 빌더 하나를 둔다. 빌더는 vanilla HTML·CSS·JavaScript 와
`canon.json` 을 지정된 출력 디렉터리에 함께 놓는다. JSON 에는 정본 YAML 전체가 아니라
탭·제목·KPI 라벨·최소 셀·색상·enum 같은 정적 화면 계약을 명시적으로 골라 넣는다.
`PROJECT_PHASE=demo` 동안에는 현재 합성 시나리오·KPI 값·신선도도 여기서 읽는다. 생성물은
손으로 고치거나 추적하지 않는다. 템플릿 엔진도 필요 없다.

A3/A2 로 전환하면 측정값과 신선도는 `export_contract` 의 기업별 JSON 으로 옮기고,
`canon.json` 은 정적 화면 계약만 남긴다. 그때 합성 고지는 phase 가 아니라 export 의
`synthetic` envelope 필드가 결정한다. 따라서 검사는 demo 의 정본 변조→빌드→렌더와,
A3/A2 의 정적 정본 변조→빌드→렌더 및 export fixture 변조→렌더로 갈린다.

빌더는 정본 경로와 출력 디렉터리를 인자로 받아야 한다. `test_single_source` 는 정본의 임시
사본을 변조하고 **같은 빌더**를 임시 출력 디렉터리에 실행한 뒤, 그 결과를 HTTP 로 열어
Chrome DOM 에 새 값이 있고 옛 값이 없는지 본다. 이 빌더는 `make check` 와 CI 의 같은
명령으로 실행되고, 발행할 때도 고정된 Python·PyYAML 환경에서 다시 실행돼야 한다.

2026-08-26 이슈 #36 의 재실행 가능한 스파이크에서 빌더가 변조된 정본을 무시하도록 만들자
새 값 부재와 옛 값 잔존을 함께 검출했고, 정본을 읽도록 되돌리자 같은 경계가 통과했다.

**구현 상태 (2026-08-26, #38·#39).** `scripts/build_screens.py` 가 정본에서 `canon.json` 과
화면을 만들고, `test_build_reads_canon.py` 가 `min_cell`·`dep_ratio`·`activated` 세 값을
변조해 JSON 과 Chrome DOM 이 함께 바뀌는지 본다. 헌 화면을 지키는
`test_single_source.py` 는 아직 별도 명령으로 남아 있으므로 새 검사는 독립된 필수 CI
명령이다. `check_dashboard` 의 빌드 산출물 경계 이동, 나머지 demo 정본 값의 변조 범위,
A3/A2 export 변조→렌더 경계는 아직 열려 있다.

Vite·React 재도입이나 새 템플릿 의존성도 두 기준을 만족시킬 수는 있지만, 이 경계에 더
보태는 것이 없다. 런타임에 원시 YAML 을 직접 읽는 안은 빌드 시점 읽기가 아니므로 탈락이다.

### 4.3 게이트 — 재분류

codex 가 "유지" 9개 중 8개를 반박했다. 검증하고 반영했다. **핵심은 "깨진다" 가 아니라
"통과하면서 아무것도 안 본다" 다.**

| 게이트 | 1판 | 2판 | 이유 |
|---|---|---|---|
| `check_contract_parses` (`check-package-consistency`) | 미분류 | **유지 + 선행 조건** | PyYAML 로 정본을 파싱한다. 빌더보다 먼저 같은 입력이 유효한 YAML 인지 보장한다 |
| `test_suppression` | 다시 쓰기(행 부재로 반전) | **다시 쓰기 — 방향이 반대** | 서버 억제는 행을 **안 없앤다.** 작은 그룹을 NULL 키 remainder 로 접는다 (기술문서 1400·1532·1665, 정본 326). 단언은 "NULL remainder 행이 있고 측정값이 억제되며 화면이 그 NULL 을 억제로 표시하고, remainder-NULL 과 UNMATCHED-NULL 이 구분된다" |
| `test_single_source` | 없애기 | **다시 쓰기 (§4.2)** | 헌 화면의 변조→렌더는 빌드 시점 읽기가 대체하지 않는다. 새 화면 역할은 아래 `test_build_reads_canon` 으로 옮겼다 |
| `test_build_reads_canon` | 없음 | **신설 (§4.2 구현)** | #38·#39가 정본→실제 빌더→`canon.json`→Chrome DOM 경계를 필수 CI 명령으로 붙였다 |
| `check_dashboard` (`check-package-consistency`) | 미분류 | **다시 쓰기 (§4.2)** | 지금은 `index.html` 의 상수·라벨·색상을 정규식으로 읽는다. 빌드 뒤에는 `canon.json` 과 실제 빌드 산출물에서 같은 정적 계약을 대조해야 한다 |
| `test_consent` | 유지 | **다시 쓰기** | `app.html` 의 상태 초기화·CSS 클래스·DOM 문구·`member-demo.json` 에 결합돼 있다. 구현 독립 경계가 아니다 |
| `test_fixtures` | 유지 | **다시 쓰기** | 파이프라인을 안 돌린다. fixture 파일 안의 산술만 재계산한다. **재구축이 fixture 를 전부 무시해도 초록불이다.** fixture 는 자산으로 유지하되 게이트는 fixture→파이프라인→기대값 대조로 바꾼다 |
| `generate_synthetic --check` | 유지 | **A3 까지 보류** | 자기 모델과 1개월 결정성만 본다. 어떤 경로도 그 출력을 소비하지 않는다 |
| `test_export_contract`(+cases) | 유지 | **A3 까지 보류** | `PROJECT_PHASE=demo` 로 명시적 SKIP. 검증기를 시험하고 생산자를 안 시험한다. 의도된 설계지만 재구축 게이트로는 공백 |
| `test_tech_sql` | 유지 | **문서 린트로 강등** | 옛 문서의 내부 정합성만 본다. Snowflake SQL 을 실행하지 않는다. §4.4 대로 문서가 잠정이면 게이트가 아니다 |
| `check_start_gates` | 유지 | **문서 린트로 강등** | 같은 문서에 묶인다. 다만 정본 스캔은 추가했다 (오늘 버그) |
| `check_awaiting_decision` (`check-package-consistency`) | 미분류 | **유지** | 결정 대기·결정 기록의 marker 가 실제 문서에 있는지 본다. 이번 프레임워크 결정도 이 경계를 통과한다 |
| `check_local_matches_ci` | 유지 | **유지 + 확장** | `.py`·`.sh` 만 인식한다. 비-Python 재구축의 네이티브 명령을 양쪽이 조용히 무시한다 |
| `check_status_headers` | 유지 | **유지 + 명시** | 상태 문서가 사라지면 조용히 return 한다. 필수 문서를 선언하거나 검사를 지운다 |
| `test_pages_root` | 유지 | **유지** | 인쇄된 QR 을 지킨다. codex 도 유일하게 무조건 안전하다고 판정 |
| `check_pages_publish_boundary` (`check-package-consistency`) | 미분류 | **유지 + 발행 경계 연결** | 현재는 리다이렉트 한 파일만 허용한다. 재구축 화면을 다시 발행하는 결정이 나면 같은 빌더 산출물이 명시적 allowlist 를 통해서만 Pages 로 가야 한다 |
| `check-forbidden-terms.sh` | 미분류 | **유지 + 경로 필수화** | `[ -f ]` 가드가 새 화면을 조용히 건너뛴다 (§4.1-2) |
| `test_doc_freshness` | **미분류** | **유지 + 선행 조건** | 헌 화면 둘이 필수 스크린샷 입력이다 (§4.1-1) |
| `check_docs` (`check-package-consistency`) | 미분류 | **유지** | 파생 문서의 필수 고지와 금지 표현을 계속 대조한다. 재구축 프레임워크와 무관하다 |
| `check_surfaces` | 미분류 | **유지 + 선행 조건** | 정본이 지운 화면을 가리키면 실패 (§4.1-3) |
| `check_repository_artifacts` (`check-package-consistency`) | 미분류 | **유지** | 추적 파일 15 MiB 상한을 지킨다. 생성된 화면 빌드 산출물은 추적하지 않는 결정과도 맞는다 |

유지 4 · 유지+수정 7 · 다시쓰기 5 · 보류 3 · 강등 2 · 신설 1 = **22.** 계산 단위는 필수
CI 명령 13개 중 `check-package-consistency` 하나를 그 안의 blocking 하위검사 10개로 펼친
것, 즉 나머지 명령 12 + 하위검사 10이다. 표의 `test_export_contract(+cases)` 는 CI 명령
둘을 한 행에 적었으므로 현재 표 21행이 검사 22개다.

**`Makefile` 과 `AGENTS.md` 는 13이라 말하고 이 절은 22라 말한다. 둘 다 맞다** — 전자는 **필수 CI 명령**을 세고 후자는 **검증 단위**를 센다 (명령 12 + `check-package-consistency` 안의 차단 하위검사 10). 같은 단어로 다른 단위를 세면 R2 가 되므로 두 곳 모두 단위를 이름으로 적었고, `check_local_matches_ci` 가 CI 명령 쪽 숫자를 실측과 대조한다. 1판은 당시 CI 명령 수 12를 그대로
검사 수라 썼다. 2판의 처음 표는 당시 21개 중 15개만 다뤘고 정합성 하위검사 여섯을
빠뜨렸으며, 그 15개마저 16이라고 잘못 합산했다. 둘 다 CI 명령과 함수 단위 검사를 섞어
센 오류다.

### 4.4 자산과 부채 — 재분류

codex 가 여섯 중 다섯을 부채로 반박했다. 검증하고 대부분 받는다.

| | 1판 | 2판 |
|---|---|---|
| 기술설계 108쪽 | 자산 | **분할.** 신원 해석·동의 모델·신뢰 경계·규제 질문 = 입력. **Snowflake 스키마·롤·정책·웨어하우스 사이징·내보내기 토폴로지 = 잠정.** 문서 스스로 가정 미확인(45행), 계정 없음(1231행), SQL 미실행(1591행)이라 적는다 |
| 정본 YAML 692줄 | 자산 | **분할.** 규제 레드라인 = 자산. 화면 이름·파일명·시나리오·내보내기 형태·구현 상태 = 재구축 종속 |
| fixture 7종 | 자산 | **명세로 자산, 게이트로는 아직 아님** (§4.3) |
| 합성 생성기 | 자산 | **A3 파이프라인 생길 때까지 부채.** 782줄이 소비자 없는 Snowflake 시대 가정을 인코딩 |
| 제안서 v12 | 자산 | **동결 산출물.** 보존하되 재구축 호환 목표로 삼지 않는다 (§4.1) |
| 학습 28건 | 자산 | **이 문서가 사용 가능한 형태다.** 원본은 근거 |

**남는 진짜 자산: 규제 레드라인, 신원·동의·신뢰경계 설계, fixture 7종의 명세, 그리고
이 문서.** 그 외는 잠정이거나 재구축 종속이다.

---

## 5. 오늘 고친 살아 있는 버그 셋

codex 헌장 검토가 계획 문제만 낸 것이 아니다. main 에 있던 버그 셋을 냈고 고쳤다.

| 버그 | 증거 |
|---|---|
| `test_suppression` 이 파싱 0건으로 통과 가능 | `seen` 이 증가만 하고 미사용, 출력은 계산 상수. 파싱 정규식 둘을 깨서 확인 — 전 `PASS — 54 states`, 후 `FAIL — zero displayed values parsed` |
| 정본 세 곳이 게이트를 아직 "넷" 이라 함 | 428·492·500행. `check_start_gates` 가 기술문서만 스캔했다. 정본 스캔 추가 후 되돌려 실패 확인 |
| CI 스텝 설명이 `FRAC.repeat` 을 말함 | 전날 그 결합을 없앴는데 설명을 안 고쳤다. G1 확장의 근거 |

`test_suppression` 은 **조용히 죽었다가 다시 세운 검사 셋 중 하나다.** 그때 넣은 방어가
그 `seen` 변수였고, 단언이 없어서 방어가 아니었다. 이번에 단언을 붙였다.

---

## 6. 1판이 틀린 자리 (기록)

| 1판 주장 | 실제 |
|---|---|
| 다섯 원인이 28건 전부를 덮는다 | 22건. 나머지 6은 도구 운영 사실 |
| 게이트 열한 개를 분류했다 | CI 명령과 정합성 하위검사를 섞어 셌다. 같은 단위와 전체 목록은 §4.3 |
| 서버 억제는 행을 없앤다 | NULL 키 remainder 로 접는다 |
| `test_single_source` 는 불필요해진다 | 변조→렌더 검사이므로 빌드 경계에서 다시 써야 한다 |
| `test_consent`·`test_fixtures` 등 9개 유지 | 8개가 목업 결합 또는 공백 |
| 108쪽 문서는 자산 | 절반이 잠정 아키텍처 |
| 생성기 897줄 | 782줄 |
| 8/25 에 게이트 다섯 개 추가 | `check_*` 함수 다섯, 필수 CI 명령은 8→12 |
| 화면 폐기 조건은 스크린샷 재현 | 게이트 넷 (§4.1) |

**1판을 codex 없이 그대로 실행했으면 재구축이 목업 결합 게이트 여덟 개와 잠정 아키텍처
문서 위에 서 있었을 것이다.**
